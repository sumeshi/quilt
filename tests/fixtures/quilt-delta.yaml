title: Delta
description: Delta test fixture
version: '1.0.0'
author: qsv test suite
stages:
  process_data:
    type: process
    steps:
      load:
        path: tests/fixtures/delta.csv
      delta:
        column: signed
        output: difference
      select:
        colnames: [signed, difference]
      head:
        number: 2
      show:
