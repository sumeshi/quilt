title: Calc
description: Calc finalizer fixture
version: '1.0.0'
author: qsv test suite
stages:
  process_data:
    type: process
    steps:
      load:
        path: tests/fixtures/calc.csv
  output_data:
    type: output
    source: process_data
    steps:
      calc:
        column: value
        sum: true
