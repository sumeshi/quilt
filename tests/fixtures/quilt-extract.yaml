title: Extract
description: Extract test fixture
version: '1.0.0'
author: qsv test suite
stages:
  process_data:
    type: process
    steps:
      load:
        path: tests/fixtures/extract.csv
      extract:
        column: value
        regex: '(?P<user>[^@]+)@(?P<domain>.+)'
      select:
        colnames: [value, user, domain]
      head:
        number: 1
      show:
