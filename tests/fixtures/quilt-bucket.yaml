title: Bucket
description: Bucket test fixture
version: '1.0.0'
author: qsv test suite
stages:
  process_data:
    type: process
    steps:
      load:
        path: tests/fixtures/bucket.csv
      cast:
        colname: when
        type: datetime
      bucket:
        column: when
        interval: 5m
        output: bucketed
      select:
        colnames: [when, bucketed]
      head:
        number: 1
      show:
