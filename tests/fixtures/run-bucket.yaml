title: Bucket
description: Bucket test fixture
version: 1
author: qlt test suite
stages:
- name: process_data
  steps:
  - load:
      paths:
      - bucket.csv
  - cast:
      column: when
      type: datetime
  - bucket:
      column: when
      interval: 5m
      output: bucketed
  - select:
      columns:
      - when
      - bucketed
  - head:
      number: 1
  - show: {}
