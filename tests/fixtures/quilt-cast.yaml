title: Cast
description: Cast test fixture
version: '1.0.0'
author: qsv test suite
stages:
  process_data:
    type: process
    steps:
      load:
        path: tests/fixtures/cast.csv
      cast:
        colname: number
        type: int
      select:
        colnames: [number]
      head:
        number: 1
      show:
